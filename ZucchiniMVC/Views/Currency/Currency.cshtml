@model Zucchinimvc.Models.ViewModels.CurrencyGraphViewModel

@{
    ViewData["Title"] = "Currency Rates";

    var selectedRates = Model.Rates
        .Where(r => new[] { "SEK", "EUR" }.Contains(r.Key))
        .ToDictionary(r => r.Key, r => r.Value);
}

<h1>Currency Rates</h1>

<p>
    Base Currency: <strong>@Model.BaseCurrency</strong>
</p>

<div style="width: 100%; max-width: 900px;">
    <canvas id="currencyChart"></canvas>
</div>
<div class="mb-3">
    <a asp-controller="Currency" asp-action="Currency" asp-route-range="1d" class="btn btn-sm btn-primary">1 Day</a>
    <a asp-controller="Currency" asp-action="Currency" asp-route-range="7d" class="btn btn-sm btn-primary">1 Week</a>
    <a asp-controller="Currency" asp-action="Currency" asp-route-range="1m" class="btn btn-sm btn-primary">1 Month</a>
</div>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<script>
    const labels = @Html.Raw(Json.Serialize(selectedRates.Keys));
    const values = @Html.Raw(Json.Serialize(selectedRates.Values));

    const ctx = document.getElementById('currencyChart');

    new Chart(ctx, {
    type: 'line',
    data: {
    labels: labels,
    datasets: [{
    label: 'Exchange Rates from @Model.BaseCurrency',
    data: values,
    borderWidth: 1
    }]
    },
    options: {
    responsive: true,
    scales: {
    y: {
    beginAtZero: true
    }
    }
    }
    });
</script>