@using Zucchinimvc.Models.ViewModels
@model IEnumerable<ArticleCardViewModel>

@{
    ViewData["Title"] = Model.FirstOrDefault()?.Article.Category?.Name ?? "Articles";
}

<section class="latest-news-section">
    <div class="section-header" style="margin-bottom: 24px;">
        <h2 class="section-title">
            @(Model.FirstOrDefault()?.Article.Category?.Name != null
        ? $"Latest {Model.First().Article.Category!.Name} News"
        : "Oops!")
        </h2>
    </div>


    @if (!Model.Any())
{
    <p style="color: #888; margin-top: 20px;">No articles found in this category.</p>
}
else
{
    <div class="news-grid">
        @foreach (var article in Model)
        {
            <partial name="_NewscardPartial" model="article" />
        }
    </div>
}
</section>