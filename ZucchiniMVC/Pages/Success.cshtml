@page
@model SuccessModel
@{
    ViewData["Title"] = "Payment Success";
}

<h2>Payment Successful!</h2>
<p>Your payment session was completed successfully.</p>
@if (!string.IsNullOrEmpty(Model.SessionId))
{
    <p>Session ID: <strong>@Model.SessionId</strong></p>
}
